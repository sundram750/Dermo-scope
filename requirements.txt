# ─────────────────────────────────────────────
# Dermo-Scope – Python Dependencies (Lightweight)
# ─────────────────────────────────────────────
# Pinned to Python 3.11 (see runtime.txt)
# Platform: Linux x86_64 (Streamlit Cloud) + Windows
# ─────────────────────────────────────────────

# Web Application
streamlit>=1.28.0

# Deep Learning Inference
# ai-edge-litert = Google's official TFLite successor
# Pinned to 1.4.0 — last stable release with Linux x86_64 wheels for Python 3.11
# DO NOT loosen this pin without checking: https://pypi.org/project/ai-edge-litert/#files
ai-edge-litert==1.4.0

# Computer Vision (headless = no GUI dependencies, safe for server)
opencv-python-headless>=4.8.0

# Image Processing
Pillow>=10.0.0

# Numerical (cap <2.0 to keep compat with ai-edge-litert C extensions)
numpy>=1.24.0,<2.0
