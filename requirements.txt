# ─────────────────────────────────────────────
# Dermo-Scope – Python Dependencies (Lightweight)
# ─────────────────────────────────────────────
# Supports Python 3.9, 3.10, 3.11, 3.12, 3.13
# Platform: Linux (Streamlit Cloud / Render / Railway) + Windows
# ─────────────────────────────────────────────

# Web Application
streamlit>=1.28.0

# Deep Learning Inference
# ai-edge-litert = Google's official TFLite successor
# Has wheels for py3.9-3.13 on Linux x86_64, aarch64, macOS, Windows
ai-edge-litert>=1.0.1

# Computer Vision (headless = no GUI dependencies, safe for server)
opencv-python-headless>=4.8.0

# Image Processing
Pillow>=10.0.0

# Numerical
numpy>=1.24.0
